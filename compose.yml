services:
  haruyq-util-bot:
    build:
      context: .
    container_name: haruyq-util-bot
    env_file:
      - ./config/.env
    volumes:
      - ./config:/haruyq-util-bot/config
      - ./db:/haruyq-util-bot/db
    restart: always